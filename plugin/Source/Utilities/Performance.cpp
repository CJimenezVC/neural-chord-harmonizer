#include "Performance.h"

// Performance.h is header-only; this translation unit exists so the build
// system has a stable object file for the utilities module and a place to add
// any out-of-line profiling sinks (e.g. CSV writers) later.
